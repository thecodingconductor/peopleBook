import {
    GET_ORGS,
    ADD_ORG,
    DELETE_ORG,
    SET_CURRENT_ORG,
    CLEAR_CURRENT_ORG,
    UPDATE_ORG,
    CLEAR_ORGS,
    CLEAR_ORG_FILTER
} from '../types';


export default (state, action) => {
    switch (action.type) {
        default:
            return state
    }
}